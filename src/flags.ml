(** Global flags for different options.
    @author Zachary Ferguson *)

(** Should the steps of interpretation be verbosly printed? *)
let verbose = ref false;;
(** Only the input and results will be printed. *)
let quite = ref false;;
